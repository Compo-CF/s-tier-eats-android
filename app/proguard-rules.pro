# Default ProGuard rules. Firebase/Firestore ship their own consumer rules,
# so nothing custom is needed yet. Add app-specific keep rules here if release
# minification later strips something reflective.
